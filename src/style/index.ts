import { StyleSheet } from "@react-pdf/renderer";

export default StyleSheet.create({
  page: { display: "flex", flexDirection: "row", fontFamily: "THSarabunNew" },
  text: {
    position: "absolute",
    fontSize: "16px",
    fontWeight: "bold",
  },
});
